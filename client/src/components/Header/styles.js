import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    appHeader: {
        marginBottom: '25px'
    },
    customLogo: {
        fontSize: '52px',
    }
});

export default useStyles;